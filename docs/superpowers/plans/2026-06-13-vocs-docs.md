# Vocs Documentation Site Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up a Vocs documentation site for rxfy as a new `apps/docs` workspace package, with real initial content drawn from the existing READMEs.

**Architecture:** A private `apps/docs` pnpm workspace package runs Vocs (Waku/Vite-based). Standard `build`/`dev`/`check-types` scripts plug into the existing Turbo task graph with no `turbo.json` changes (Vocs outputs to `dist/`, already a configured output). Content is static MDX under `src/pages/`; the docs package has no dependency on the rxfy libraries.

**Tech Stack:** Vocs, Waku, Vite, React, MDX, TypeScript, pnpm workspaces, Turborepo.

**Spec:** `docs/superpowers/specs/2026-06-13-vocs-docs-design.md`

**No test framework applies** — a docs site has no unit tests. "Verification" steps run the real Vocs build / dev server and assert exit status and rendered output. Treat a clean `vocs build` as the passing condition for each content task.

---

### Task 1: Scaffold the `apps/docs` workspace package

**Files:**

- Modify: `pnpm-workspace.yaml`
- Create: `apps/docs/package.json`
- Create: `apps/docs/tsconfig.json`
- Create: `apps/docs/vocs.config.ts`
- Create: `apps/docs/src/pages/index.mdx` (temporary boot page, replaced in Task 2)

- [ ] **Step 1: Add the `apps/*` workspace glob**

Edit `pnpm-workspace.yaml` to:

```yaml
packages:
  - "packages/*"
  - "examples/*"
  - "apps/*"
```

- [ ] **Step 2: Create the minimal package manifest**

Create `apps/docs/package.json`:

```json
{
  "name": "docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vocs dev",
    "build": "vocs build",
    "preview": "vocs preview",
    "check-types": "tsc --noEmit"
  }
}
```

- [ ] **Step 3: Install runtime + dev dependencies**

Run (from repo root):

```bash
pnpm --filter docs add vocs waku vite react react-dom
pnpm --filter docs add -D @types/react @types/react-dom typescript @vanya2h/typescript-config
```

Expected: pnpm resolves and writes the versions into `apps/docs/package.json` `dependencies` / `devDependencies`, updates `pnpm-lock.yaml`, no errors.

- [ ] **Step 4: Create the tsconfig**

Create `apps/docs/tsconfig.json` (mirrors `packages/rxfy-react/tsconfig.json`):

```json
{
  "extends": "@vanya2h/typescript-config/react",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["./**/*.ts", "./**/*.tsx", "./vocs.config.ts"],
  "exclude": ["./node_modules/**/*", "./.turbo/**/*", "./dist/**/*"]
}
```

- [ ] **Step 5: Create the Vocs config**

Create `apps/docs/vocs.config.ts`:

```ts
import { defineConfig } from "vocs/config";

export default defineConfig({
  title: "rxfy",
  description: "Stream-based state management built on RxJS",
  sidebar: [
    { text: "Introduction", link: "/" },
    { text: "Getting Started", link: "/getting-started" },
    {
      text: "Core Concepts",
      items: [
        { text: "Atom", link: "/core-concepts/atom" },
        { text: "Lens", link: "/core-concepts/lens" },
        { text: "Edge", link: "/core-concepts/edge" },
        { text: "Wrapped", link: "/core-concepts/wrapped" },
      ],
    },
    { text: "Models & State", link: "/models-state" },
    { text: "React Bindings", link: "/react" },
    { text: "Server-Side Rendering", link: "/ssr" },
  ],
});
```

- [ ] **Step 6: Create a temporary boot page**

Create `apps/docs/src/pages/index.mdx`:

```mdx
# rxfy

Documentation scaffold — content lands in the next tasks.
```

- [ ] **Step 7: Verify the build succeeds**

Run: `pnpm --filter docs build`
Expected: exits 0, prints a Vocs build summary, and creates `apps/docs/dist/` with HTML output.

- [ ] **Step 8: Verify the dev server boots, then stop it**

Run: `pnpm --filter docs dev` (Ctrl-C after it prints the local URL)
Expected: prints `http://localhost:5173` (or the next free port) and serves without error.

- [ ] **Step 9: Add a `.vocs` gitignore entry if Vocs created a cache dir**

Run: `ls -a apps/docs`
If a `.vocs` directory exists, append it to the root `.gitignore` (a new line `.vocs`). `dist` and `.turbo` are already ignored globally — do nothing for those.

- [ ] **Step 10: Commit**

```bash
git add pnpm-workspace.yaml pnpm-lock.yaml apps/docs .gitignore
git commit -m "feat(docs): scaffold Vocs documentation site"
```

---

### Task 2: Introduction & Getting Started pages

**Files:**

- Modify: `apps/docs/src/pages/index.mdx` (replace boot content)
- Create: `apps/docs/src/pages/getting-started.mdx`

- [ ] **Step 1: Write the Introduction page**

Replace `apps/docs/src/pages/index.mdx` with:

````mdx
# rxfy

**rxfy** (/ɑɹ ɪks faɪ/) — stream-based state management built on RxJS: normalized
entities, reactive queries, and first-class SSR.

State is normalized: `data$` emits entity **ids**, and entity data lives in model
stores. Components render lists by id and subscribe per entity — so a single
`store.set` (e.g. from a websocket) updates every subscriber without re-fetching
anything.

## Quick taste

```tsx
import { useMemo } from "react";
import { z } from "zod";
import { createModel, defineState, array } from "rxfy";
import { StoreProvider, useStateData, useModelStore, Pending } from "rxfy-react";

const Todo = createModel(z.object({ id: z.string(), title: z.string(), done: z.boolean() }), {
  getKey: (t) => t.id,
  name: "todo",
});

const todosState = defineState({
  key: "todos",
  params: z.object({ filter: z.enum(["all", "active", "done"]) }),
  model: { todos: array(Todo) },
  mutations: {
    addTodo: (prev, todo: { id: string; title: string; done: boolean }) => ({
      ...prev,
      todos: [...prev.todos, todo],
    }),
  },
});

function TodoApp() {
  const { data$, mutations } = useStateData(todosState, fetchTodos, { filter: "all" });
  return (
    <Pending value$={data$} pending={<p>Loading...</p>}>
      {({ todos }) => (
        <ul>
          {todos.map((id) => (
            <TodoItem key={id} id={id} />
          ))}
        </ul>
      )}
    </Pending>
  );
}

function TodoItem({ id }: { id: string }) {
  const store = useModelStore(Todo);
  const todo$ = useMemo(() => store.get(id), [store, id]);
  return <Pending value$={todo$}>{(todo) => <li>{todo.title}</li>}</Pending>;
}
```

Continue to [Getting Started](/getting-started).
````

- [ ] **Step 2: Write the Getting Started page**

Create `apps/docs/src/pages/getting-started.mdx`:

````mdx
# Getting Started

## Install

```bash
npm install rxfy rxfy-react
# peer deps: rxjs zod lodash react react-dom @types/react
```

`rxfy` peer dependencies: `rxjs ^7`, `zod ^3`, `lodash ^4`. `rxfy-react` additionally
needs `react`/`react-dom` 18+ (and `next >=14` only for the `rxfy-react/next` subpath).

## Wrap your app

`StoreProvider` creates the model registry that `useStateData` and `useModelStore`
write to and read from.

```tsx
import { StoreProvider } from "rxfy-react";
import { createRoot } from "react-dom/client";

createRoot(document.getElementById("root")!).render(
  <StoreProvider>
    <App />
  </StoreProvider>,
);
```

A plain client-only app passes no props to `StoreProvider`. The SSR props
(`ssr`, `registry`, `dehydratedState`) are covered in
[Server-Side Rendering](/ssr).

## Next steps

- [Models & State](/models-state) — the high-level API for normalized data.
- [React Bindings](/react) — hooks and rendering helpers.
- [Core Concepts](/core-concepts/atom) — the reactive primitives underneath.
````

- [ ] **Step 3: Verify the build**

Run: `pnpm --filter docs build`
Expected: exits 0; `apps/docs/dist/getting-started` (or `getting-started.html`) is produced. No MDX parse errors.

- [ ] **Step 4: Commit**

```bash
git add apps/docs/src/pages/index.mdx apps/docs/src/pages/getting-started.mdx
git commit -m "docs(docs): add introduction and getting started pages"
```

---

### Task 3: Core Concepts pages (Atom, Lens, Edge, Wrapped)

**Files:**

- Create: `apps/docs/src/pages/core-concepts/atom.mdx`
- Create: `apps/docs/src/pages/core-concepts/lens.mdx`
- Create: `apps/docs/src/pages/core-concepts/edge.mdx`
- Create: `apps/docs/src/pages/core-concepts/wrapped.mdx`

- [ ] **Step 1: Atom page**

Create `apps/docs/src/pages/core-concepts/atom.mdx`:

````mdx
# Atom

A reactive cell — extends `Observable<T>` with synchronous `get()`, `set()`, and
`modify()` backed by a `BehaviorSubject`. The fundamental reactive primitive.

```ts
import { createAtom } from "rxfy";

const count = createAtom(0);

count.get(); // 0
count.set(5);
count.modify((n) => n + 1);
count.get(); // 6

count.subscribe((n) => console.log(n)); // emits current value, then future changes
```

## Signature

```ts
function createAtom<T>(value: T): Atom<T>;
// Atom<T>: Observable<T> & {
//   get(): T;
//   set(val: T): void;
//   modify(fn: (val: T) => T): void;
// }
```
````

- [ ] **Step 2: Lens page**

Create `apps/docs/src/pages/core-concepts/lens.mdx`:

````mdx
# Lens

A focused, bidirectional view into an `Atom`. Reads and writes propagate in both
directions. Change detection uses `lodash.isEqual`.

```ts
import { createAtom, createLens, keyLens } from "rxfy";

const user = createAtom({ id: "1", name: "Alice" });
const name = createLens(user, keyLens("name"));

name.get(); // "Alice"
name.set("Bob");
user.get(); // { id: "1", name: "Bob" }
```

## Signatures

```ts
function createLens<S, T>(source$: IAtom<S>, lens: ILens<S, T>): Lens<S, T>;

function keyLens<S, K extends keyof S>(key: K): ILens<S, S[K]>;
// ILens<S, T>: { get(source: S): T; set(current: T, source: S): S }
```
````

- [ ] **Step 3: Edge page**

Create `apps/docs/src/pages/core-concepts/edge.mdx`:

````mdx
# Edge

Manages an async data load lifecycle. Queues the load via `p-queue`, tracking
`IDLE → PENDING → FULFILLED | REJECTED` state in an `Atom`.

```ts
import PQueue from "p-queue";
import { of } from "rxjs";
import { createAtom, createEdge, createIdle } from "rxfy";

const queue = new PQueue({ concurrency: 5 });
const state$ = createAtom(createIdle<{ id: string }>());
const edge = createEdge(state$, queue, () => of({ id: "42" }));

edge.toObservable().subscribe((user) => console.log(user));
await edge.next(); // trigger a reload
```

## Signature

```ts
function createEdge<T>(state$: IAtom<IEdgeState<T>>, queue: PQueue, loader: () => Observable<T>): IEdge<T>;

// IEdge<T>: {
//   subject$: IAtom<IEdgeState<T>>;
//   toObservable(): Observable<T>;
//   next(): Promise<IWrapped<T, StatusEnum.FULFILLED | StatusEnum.REJECTED>>;
// }
```

For React, prefer `useStateData` (see [React Bindings](/react)) over wiring Edges by
hand. The status union it tracks is described in [Wrapped](/core-concepts/wrapped).
````

- [ ] **Step 4: Wrapped page**

Create `apps/docs/src/pages/core-concepts/wrapped.mdx`:

````mdx
# Wrapped

`IWrapped` is the discriminated union for async state — the `IDLE / PENDING /
FULFILLED / REJECTED` pattern used throughout rxfy (it is what an [Edge](/core-concepts/edge)
tracks). Construct values with the helpers:

```ts
import { createIdle, createPending, createFulfilled, createRejected } from "rxfy";

createIdle<string>(); // { type: "IDLE" }
createPending<string>(); // { type: "PENDING" }
createFulfilled("hi"); // { type: "FULFILLED", value: "hi" }
createRejected("oops"); // { type: "REJECTED", error: "oops" }
```

## Helpers

| Helper                      | Returns                        |
| --------------------------- | ------------------------------ |
| `createIdle<T>()`           | `{ type: "IDLE" }`             |
| `createPending<T>()`        | `{ type: "PENDING" }`          |
| `createFulfilled<T>(value)` | `{ type: "FULFILLED", value }` |
| `createRejected<T>(error)`  | `{ type: "REJECTED", error }`  |

## Signatures

```ts
function createIdle<T>(): IWrapped<T, StatusEnum.IDLE>;
function createPending<T>(): IWrapped<T, StatusEnum.PENDING>;
function createFulfilled<T>(value: T): IWrapped<T, StatusEnum.FULFILLED>;
function createRejected<T>(error: unknown): IWrapped<T, StatusEnum.REJECTED>;
```
````

- [ ] **Step 5: Verify the build**

Run: `pnpm --filter docs build`
Expected: exits 0; the four `core-concepts/*` pages build with no MDX errors.

- [ ] **Step 6: Commit**

```bash
git add apps/docs/src/pages/core-concepts
git commit -m "docs(docs): add core concepts pages (atom, lens, edge, wrapped)"
```

---

### Task 4: Models & State and React Bindings pages

**Files:**

- Create: `apps/docs/src/pages/models-state.mdx`
- Create: `apps/docs/src/pages/react.mdx`

- [ ] **Step 1: Models & State page**

Create `apps/docs/src/pages/models-state.mdx`:

````mdx
# Models & State

The high-level API: define typed state shapes, normalize entities, and mutate state
without manual bookkeeping.

## `createModel`

Creates a typed model descriptor for normalizing and sharing entities across state
slices.

```ts
import { z } from "zod";
import { createModel } from "rxfy";

const TodoModel = createModel(z.object({ id: z.string(), title: z.string(), done: z.boolean() }), {
  getKey: (todo) => todo.id,
  name: "todo",
});
```

```ts
function createModel<T>(schema: z.ZodType<T>, opts: { getKey: (item: T) => string; name?: string }): ModelDescriptor<T>;
```

`name` is the model's stable string identity for SSR — only named models are included
in `dehydrate` output. Models without a name work normally but opt out of SSR
serialization (a dev warning fires if they hold data at dehydrate time).

## `defineState`

Defines a typed state shape: fetch params schema, model fields, and optional mutations.

```ts
import { z } from "zod";
import { defineState, array } from "rxfy";

const todosState = defineState({
  key: "todos", // stable identity for the SSR query cache; omit to opt out of caching
  params: z.object({ filter: z.enum(["all", "active", "done"]) }),
  model: { todos: array(TodoModel) },
  mutations: {
    addTodo: (prev, todo) => ({ ...prev, todos: [...prev.todos, todo] }),
  },
});
```

```ts
function defineState<TParams, TFields, TMutations>(def: {
  key?: string;
  params: z.ZodType<TParams>;
  model: TFields;
  mutations?: TMutations;
}): StateDescriptor<TParams, ShapeFromFields<TFields>, TMutations>;
```

Mutation reducers operate on the full fetch shape (entities). When invoked through
`useStateData`, rxfy denormalizes the current ids into fresh entities, runs the reducer,
and normalizes the result back into model stores and ids. The normalized query shape —
what `data$` emits — is derived as `QueryShapeOf<TShape>`: array fields become `string[]`
(entity keys), single fields become `string`.

## `array` / `single`

Field descriptor helpers — declare whether a `defineState` model field holds an array or
a single item.

```ts
import { array, single } from "rxfy";

const userPageState = defineState({
  params: z.object({ userId: z.string() }),
  model: {
    user: single(UserModel), // one item
    friends: array(UserModel), // array of items
  },
});
```

```ts
function array<T>(model: ModelDescriptor<T>): FieldDescriptor<T[]>;
function single<T>(model: ModelDescriptor<T>): FieldDescriptor<T>;
```

## Low-level storage

`createModelRegistry` / `createModelStore` are the normalized storage primitives. In
React apps they are wired automatically by `StoreProvider`; use them directly for
non-React or custom setups.

```ts
import { createModelRegistry } from "rxfy";

const registry = createModelRegistry();
const users = registry.model(UserModel);

users.set("1", { id: "1", name: "Alice" });
users.get("1").subscribe(console.log); // emits { id: "1", name: "Alice" }
```

> `get()` on a key that has never had `set()` called returns an Observable that never
> emits until `set()` or `setMany()` is called for that key.
````

- [ ] **Step 2: React Bindings page**

Create `apps/docs/src/pages/react.mdx`:

````mdx
# React Bindings

`rxfy-react` is the official React binding for rxfy. Wrap your tree in `StoreProvider`
(see [Getting Started](/getting-started)), then use the hooks below.

## `useStateData`

Fetches data, normalizes entities into model stores, and returns a `StateHandle`.
**`data$` emits the normalized query shape — entity ids, not entities** (`array` fields
→ `string[]`, `single` fields → `string`). Render lists by id and read entity data
through `useModelStore`.

```tsx
import { useMemo, useState } from "react";
import { useStateData, useModelStore, Pending } from "rxfy-react";
import { todosState, fetchTodos, TodoModel } from "./todos";

function TodoApp() {
  const [filter, setFilter] = useState<"all" | "active" | "done">("all");
  const params = useMemo(() => ({ filter }), [filter]);

  const { data$, mutations, reload } = useStateData(todosState, fetchTodos, params);

  return (
    <Pending value$={data$} pending={<p>Loading...</p>}>
      {({ todos }) => (
        <>
          <ul>
            {todos.map((id) => (
              <TodoItem key={id} id={id} />
            ))}
          </ul>
          <button onClick={() => mutations.addTodo({ id: crypto.randomUUID(), title: "new", done: false })}>Add</button>
          <button onClick={reload}>Reload</button>
        </>
      )}
    </Pending>
  );
}
```

```ts
function useStateData<TParams, TShape, TMutations>(
  state: StateDescriptor<TParams, TShape, TMutations>,
  fetchFn: (params: TParams, signal: AbortSignal) => Promise<TShape>,
  params: TParams,
): StateHandle<TShape, TMutations>;

// StateHandle: {
//   data$: Observable<QueryShapeOf<TShape>>;  // ids only
//   set: (value: TShape | ((prev: TShape) => TShape)) => void;  // full entities
//   reload: () => void;
//   mutations: BoundMutations<TShape, TMutations>;  // full entities
// }
```

**Caching** (states with a `key`): results, mutations, and `set` write through to the
registry's query cache, so a remount with the same params starts from cached ids without
re-fetching — while entity values always come live from model stores. `reload()` deletes
the cache entry and re-fetches. Keyless states skip the cache and fetch per mount.

## `useModelStore`

Returns the `ModelStore` for a model descriptor, so a component can subscribe to a single
normalized entity without re-fetching the full list.

```tsx
import { useMemo } from "react";
import { useModelStore, Pending } from "rxfy-react";
import { TodoModel } from "./models";

function TodoItem({ id }: { id: string }) {
  const store = useModelStore(TodoModel);
  const todo$ = useMemo(() => store.get(id), [store, id]);
  return <Pending value$={todo$}>{(todo) => <li>{todo.title}</li>}</Pending>;
}
```

```ts
function useModelStore<T>(descriptor: ModelDescriptor<T>): ModelStore<T>;
```

## `Pending`

Subscribes to any `ObservableLike<T>` and renders pending / rejected / fulfilled UI. The
`rejected` render prop receives `{ status, error, onReload }` for retry flows. If
`rejected` is omitted it renders nothing and logs the error to `console.error`.

```tsx
import { Pending } from "rxfy-react";

<Pending
  value$={data$}
  pending={<p>Loading...</p>}
  rejected={({ error, onReload }) => (
    <p>
      Error: {String(error)} <button onClick={onReload}>Retry</button>
    </p>
  )}
>
  {(value) => <div>{JSON.stringify(value)}</div>}
</Pending>;
```

> `Pending` is powered by the lower-level `usePending` hook, which returns an
> `IPendingStatus<T>` union (`pending` / `rejected` / `fulfilled`). The `source$` you pass
> must be referentially stable across renders — `data$` from `useStateData` already is.
````

- [ ] **Step 3: Verify the build**

Run: `pnpm --filter docs build`
Expected: exits 0; `models-state` and `react` pages build with no MDX errors.

- [ ] **Step 4: Commit**

```bash
git add apps/docs/src/pages/models-state.mdx apps/docs/src/pages/react.mdx
git commit -m "docs(docs): add models & state and react bindings pages"
```

---

### Task 5: Server-Side Rendering page + repo-wide verification

**Files:**

- Create: `apps/docs/src/pages/ssr.mdx`

- [ ] **Step 1: SSR page**

Create `apps/docs/src/pages/ssr.mdx`:

````mdx
# Server-Side Rendering

SSR is on-demand — there is no prefetch API. Components declare their data with
`useStateData` exactly as on the client; on the server (`<StoreProvider ssr>`) a cache
miss suspends until the fetch settles. Results (fulfilled or rejected) are captured in the
registry, serialized into the HTML, and ingested on the client so the first paint is
already fulfilled: no loading flash, no re-fetch, no hydration mismatch.

**Requirements:** give models a `name` and states a `key` (stable string identities), and
write `fetchFn` to work in both environments (it runs on the server during SSR and on the
client for reloads).

## Buffered mode (any Node server)

Wait for every Suspense boundary with `onAllReady`, then send the complete document. This
is the recommended non-Next mode.

```tsx
import { renderToPipeableStream } from "react-dom/server";
import { createModelRegistry, dehydrate, hydrationScript } from "rxfy";
import { StoreProvider } from "rxfy-react";

const registry = createModelRegistry(); // one per request
renderToPipeableStream(
  <StoreProvider registry={registry} ssr>
    <App />
  </StoreProvider>,
  {
    onAllReady() {
      // collect the stream to a string, then inject:
      //   template.replace("<!--app-state-->", hydrationScript(dehydrate(registry)))
    },
  },
);
```

```tsx
// client — StoreProvider ingests the injected script automatically
import { hydrateRoot } from "react-dom/client";

hydrateRoot(
  document.getElementById("root")!,
  <StoreProvider ssr>
    <App />
  </StoreProvider>,
);
```

## Streaming mode (Next.js App Router)

`rxfy-react/next` ships `<HydrationStream />`: on each stream flush it emits newly settled
queries and entities as `window.__RXFY_SSR__.push(...)` script tags; the client
`StoreProvider` ingests them, including chunks arriving after hydration starts.

```tsx
// app/providers.tsx
"use client";
import { StoreProvider } from "rxfy-react";
import { HydrationStream } from "rxfy-react/next";

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <StoreProvider ssr>
      <HydrationStream />
      {children}
    </StoreProvider>
  );
}
```

`next` is an optional peer dependency — only this subpath needs it.

## Two-pass mode (strict `renderToString`)

For environments without React stream APIs, `collectStateData` loops render passes until
nothing suspends (each fetch waterfall level costs one extra pass):

```ts
import { renderToString } from "react-dom/server";
import { collectStateData } from "rxfy-react";

const html = await collectStateData(registry, () =>
  renderToString(<StoreProvider registry={registry} ssr><App /></StoreProvider>),
);
const state = hydrationScript(dehydrate(registry));
```

## Error handling

A `fetchFn` rejection on the server is captured as a serialized rejected entry
(`{ name, message }`, stack stripped) and hydrates as rejected state — the server HTML
shows your `<Pending rejected>` UI, and its `onReload` retries client-side with a real
fetch.
````

- [ ] **Step 2: Verify the docs build**

Run: `pnpm --filter docs build`
Expected: exits 0; every sidebar page (index, getting-started, 4 core-concepts, models-state, react, ssr) is present in `apps/docs/dist`.

- [ ] **Step 3: Verify the whole repo build still passes**

Run: `turbo build`
Expected: all packages build successfully, including `docs`. Libraries (`rxfy`, `rxfy-react`, `utils`) are unaffected.

- [ ] **Step 4: Verify types**

Run: `pnpm --filter docs check-types`
Expected: exits 0 (`vocs.config.ts` typechecks against the React tsconfig preset).

- [ ] **Step 5: Spot-check the dev server**

Run: `pnpm --filter docs dev`, open `http://localhost:5173`, click through every sidebar entry to confirm each page renders and internal links resolve. Ctrl-C when done.

- [ ] **Step 6: Commit**

```bash
git add apps/docs/src/pages/ssr.mdx
git commit -m "docs(docs): add server-side rendering page"
```

---

## Self-Review

**Spec coverage:**

- `apps/docs` placement + `apps/*` workspace glob → Task 1 (Steps 1–2). ✓
- Private, non-published, no rxfy deps → Task 1 Step 2 (`"private": true`) + Step 3 (only vocs/waku/vite/react installed). ✓
- Standard `build`/`dev`/`preview`/`check-types` scripts, no `turbo.json` change → Task 1 Step 2; verified Task 5 Steps 3–4. ✓
- `vocs.config.ts` with corrected sidebar → Task 1 Step 5. ✓
- tsconfig extends shared React preset → Task 1 Step 4. ✓
- `.gitignore` `.vocs` only if produced → Task 1 Step 9. ✓
- All eight pages (index, getting-started, atom, lens, edge, wrapped, models-state, react, ssr) → Tasks 2–5. ✓
- Verification (install, dev, build, repo-wide turbo build) → Task 1 Steps 7–8, Task 5 Steps 2–5. ✓

**Placeholder scan:** No TBD/TODO; every page step contains the full MDX content. ✓

**Type/name consistency:** Page links match `vocs.config.ts` sidebar links (`/core-concepts/atom`, `/models-state`, `/react`, `/ssr`). Package name `docs` is used consistently in every `pnpm --filter docs` command. ✓
