# rxfy-protocol

## 2.0.0-rc.0

### Minor Changes

- 5029f3c: Add `rxfy-protocol`: the standalone, zero-dependency wire contract for rxfy live updates — `ServerMessage`/`ClientMessage` types, `PROTOCOL_VERSION`, message constructors, and `serialize`/`parseServerMessage`/`parseClientMessage` codec.
