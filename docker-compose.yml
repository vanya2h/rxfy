services:
  docs:
    build:
      context: .
      dockerfile: apps/docs/Dockerfile
    image: rxfy-docs
    ports:
      - "3000:3000"
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      NODE_ENV: production
    restart: unless-stopped
