# `rxfy-typescript-config`

A set of useful typescript configurations:

- base — basic es6 support
- lib — good for polymorphic libs
- node — base but extended with node.js types
- react — react-compatible es6
